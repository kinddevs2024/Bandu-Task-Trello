export const BRAND_COLORS = {
  primary: '#FFD54A',
  secondary: '#FF6B6B',
  dark: '#050505',
} as const;

export const GRADIENT = `linear-gradient(90deg, ${BRAND_COLORS.primary}, ${BRAND_COLORS.secondary}, ${BRAND_COLORS.dark})`;